import 'package:flutter/material.dart';import 'models/medication.dart';
class DrugSafetyPage extends StatelessWidget{final Medication medication;const DrugSafetyPage({super.key,required this.medication});@override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text('Medication safety')),body:ListView(padding:const EdgeInsets.all(22),children:[Text(medication.name,style:const TextStyle(fontSize:30,fontWeight:FontWeight.w900)),Text('${medication.dosage} • ${medication.dosageForm}',style:const TextStyle(color:Colors.grey,fontSize:16)),const SizedBox(height:22),_card(Icons.fact_check_outlined,'Your instructions',[if(medication.instructions.isNotEmpty)medication.instructions,if(medication.foodInstruction!='No preference')medication.foodInstruction,if(medication.prescriber.isNotEmpty)'Prescriber: ${medication.prescriber}']),const SizedBox(height:14),_card(Icons.shield_outlined,'Drug information connection',['DoseBuddy is prepared to display verified drug instructions, common side effects, warnings, and interaction results here once an authoritative medication knowledge source is connected.','No interaction result is being inferred from your medication list in this prototype.']),const SizedBox(height:14),Container(padding:const EdgeInsets.all(18),decoration:BoxDecoration(color:const Color(0xFFFFF4DE),borderRadius:BorderRadius.circular(18)),child:const Text('For questions about interactions, missed doses, side effects, or changing how you take a medication, use the instructions from your prescription and contact a pharmacist or clinician.',style:TextStyle(height:1.45)))]));Widget _card(IconData i,String t,List<String>b)=>Container(padding:const EdgeInsets.all(18),decoration:BoxDecoration(color:Colors.white,borderRadius:BorderRadius.circular(20)),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Row(children:[Icon(i,color:const Color(0xFF5B67CA)),const SizedBox(width:10),Text(t,style:const TextStyle(fontSize:17,fontWeight:FontWeight.w800))]),const SizedBox(height:12),...b.where((x)=>x.isNotEmpty).map((x)=>Padding(padding:const EdgeInsets.only(bottom:8),child:Text(x,style:const TextStyle(height:1.4,color:Color(0xFF62687B)))))]));}
